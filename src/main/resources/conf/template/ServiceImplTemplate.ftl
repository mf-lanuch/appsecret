package com.${packageName}.${modelName}.services.impl;

import org.springframework.stereotype.Service;
import com.app.secret.core.base.serviceimpl.BaseServiceImpl;
import com.${packageName}.${modelName}.entity.${className};
import com.${packageName}.${modelName}.mapper.${className}Mapper;
import com.${packageName}.${modelName}.services.${className}Service;

@Service
public class ${className}ServiceImpl extends BaseServiceImpl<${className}, ${className}Mapper> implements ${className}Service {

}

